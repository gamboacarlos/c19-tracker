import React, { FC } from 'react';
import { HomeTemplate } from '../../templates';

const HomePage: FC = () => {
  return <HomeTemplate mainTitleData="Track the covid-19 all over the world" />;
};

export default HomePage;
